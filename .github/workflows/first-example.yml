name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v4

            - name: List and read file
              run: |
                  echo "My frist github Action job"
                  ls -ltra
                  cat README.md

            - name: Generate ASCII artwork
              run: cowsay -f dragon "Run for cover, I am a DRAGCN.. ..RAWR" >> dragon.txt

            # - name: List files
            #   run: ls

            # - name: Read files
            #   run: cat README.md